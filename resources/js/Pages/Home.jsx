import Layout from "@/Layouts/Layout.jsx";

const Home = ({name}) => {
    return (
        <>
            <h1 className="text-3xl font-bold underline">Hello {name}</h1>
        </>
    );
};
export default Home;
